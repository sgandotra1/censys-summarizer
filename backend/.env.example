# Censys Host Summarizer Backend Configuration
# 
# The system automatically switches between two modes:
# 1. MOCK MODE (default): Rule-based analysis, no API key needed
# 2. AI MODE: Real OpenAI analysis, requires valid API key below

# OpenAI Configuration (for AI mode)
# Get your API key from: https://platform.openai.com/account/api-keys
# Uncomment and add your real key to enable AI analysis:
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model (optional)
# Available models: gpt-4o-mini, gpt-4o, gpt-4, gpt-3.5-turbo
MODEL=gpt-4o-mini

# HOW TO SWITCH TO AI MODE:
# 1. Get OpenAI API key from https://platform.openai.com/
# 2. Copy this file: cp .env.example .env
# 3. Edit .env and replace "your_openai_api_key_here" with your real key
# 4. Restart the backend: python app.py
# 5. You'll see "AI MODE" instead of "MOCK MODE" in the startup message
